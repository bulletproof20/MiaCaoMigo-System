# Comments — Bootstrap

Reserved layer for `COMMENT ON` metadata related to Bootstrap assets.
Currently applied via Schema and Services comment loaders; extend here when Bootstrap-specific comments are split out.
