# Comments — Tests

Reserved layer for `COMMENT ON` metadata related to Tests assets.
Currently applied via Schema and Services comment loaders; extend here when Tests-specific comments are split out.
