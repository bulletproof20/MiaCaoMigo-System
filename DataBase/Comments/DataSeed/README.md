# Comments — DataSeed

Reserved layer for `COMMENT ON` metadata related to DataSeed assets.
Currently applied via Schema and Services comment loaders; extend here when DataSeed-specific comments are split out.
